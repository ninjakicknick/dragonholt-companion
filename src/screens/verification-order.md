# Verification order

For each extracted screen: compare initial render, exercise all mutations, reload to verify persistence, import/export a save where relevant, then check narrow and wide layouts before deleting legacy code.
