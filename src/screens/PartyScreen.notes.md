# Party extraction decisions

The first extracted screen is intentionally boring from a product perspective: it mirrors the existing Party experience rather than redesigning it. This gives the project a clean regression boundary.

The story-point matrix is now its own component because it is both visually dense and a likely future quick-action surface for Active Adventure. Accessibility metadata was added without changing the interaction model.
