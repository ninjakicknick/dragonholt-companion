# Legacy removal order

Remove legacy render functions only after their extracted replacement is wired and verified. Party first, then Heroes, Village, Achievements. Delete duplicated constants/handlers only when no remaining legacy screen depends on them.
