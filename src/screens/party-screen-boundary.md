# Party screen boundary

Inputs: party state, party setter, story-point toggle action.

Outputs: user interactions only. The screen does not load, save, migrate, or export campaign data. This makes it reusable inside the future Active Adventure shell without coupling it to persistence.
