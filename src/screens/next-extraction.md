# Next extraction

After Party is wired into `App.jsx`, extract Heroes. Hero editing has the most local interaction state, so separating it before Village will remove the largest remaining source of UI complexity from the shell.
