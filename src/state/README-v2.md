# Campaign state v2

The v2 state API gives the app shell one persisted campaign source and one composed action surface. Existing local saves remain normalized through `campaignSave.js`; UI screens consume state/actions rather than storage details.
