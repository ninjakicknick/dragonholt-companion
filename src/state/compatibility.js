// Compatibility boundary for the pre-2.0 app.
// Keep these names stable while App.jsx is extracted so old saves and the
// existing UI can migrate independently.
export const LEGACY_STORAGE_KEY = 'dragonholt-companion-save-v1';
export const LEGACY_SCREEN_IDS = ['party', 'heroes', 'village', 'achievements'];
