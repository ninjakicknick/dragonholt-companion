import { useMemo,useState } from 'react';
import { ArrowLeft,ArrowRight,Check,Feather,Shield,Sparkles } from 'lucide-react';
import { SKILLS } from '../data/gameData'; import { CLASSES,RACES,staminaForSkillCount } from '../data/characterCreation';
const STEPS=['Race','Class','Describe','Skills','Review'];
export default function CharacterCreator({onCancel,onComplete}){const[step,setStep]=useState(0);const[draft,setDraft]=useState({race:'',class:'',name:'',age:'',gender:'',physicalDescription:'',personality:'',background:'',skills:[]});const raceSkills=RACES[draft.race]?.skills||[];const classSkills=CLASSES[draft.class]?.skills||[];const maxStamina=staminaForSkillCount(draft.skills.length);const raceCount=draft.skills.filter(s=>raceSkills.includes(s)).length;const classCount=draft.skills.filter(s=>classSkills.includes(s)).length;const skillsValid=draft.skills.length>=5&&draft.skills.length<=8&&raceCount>=2&&classCount>=2;const canContinue=[Boolean(draft.race),Boolean(draft.class),Boolean(draft.name.trim()),skillsValid,true][step];const toggleSkill=skill=>setDraft(c=>{const selected=c.skills.includes(skill);if(!selected&&c.skills.length>=8)return c;return{...c,skills:selected?c.skills.filter(i=>i!==skill):[...c.skills,skill]}});const notes=useMemo(()=>[draft.physicalDescription&&`Physical Description\n${draft.physicalDescription}`,draft.personality&&`Personality & Ideals\n${draft.personality}`,draft.background&&`Background\n${draft.background}`].filter(Boolean).join('\n\n'),[draft]);const finish=()=>onComplete({name:draft.name.trim(),race:draft.race,class:draft.class,age:draft.age,gender:draft.gender,maxStamina,currentStamina:maxStamina,exp:0,skills:draft.skills,disabledSkills:[],items:'',notes,physicalDescription:draft.physicalDescription,personality:draft.personality,background:draft.background});
return <div className="max-w-3xl mx-auto animate-in fade-in duration-300"><button onClick={onCancel} className="flex items-center gap-1 text-[#7b2d2d] font-bold mb-4"><ArrowLeft size={18}/> Cancel</button><div className="paper-panel rounded-2xl overflow-hidden"><div className="px-5 py-6 sm:px-7 border-b border-[#cbb99b] bg-[linear-gradient(145deg,#eee1c7,#f8efd9)]"><div className="ink-label flex items-center gap-2 text-[9px] text-[#8b4b42]"><Feather size={14}/> Create a Hero</div><h2 className="display-serif font-bold text-3xl text-[#3f3028] mt-1">{STEPS[step]}</h2><div className="flex gap-1.5 mt-5">{STEPS.map((label,index)=><div key={label} className={`h-1 flex-1 rounded-full ${index<=step?'bg-[#7b2d2d]':'bg-[#d2c2a5]'}`}/>)}</div><div className="ink-label text-[8px] text-[#857363] mt-2">Chapter {step+1} of {STEPS.length}</div></div><div className="p-5 sm:p-7">{step===0&&<ChoiceGrid title="Choose a race" intro="Your race helps establish your hero's identity and suggests natural talents." choices={RACES} value={draft.race} onChange={race=>setDraft({...draft,race})}/>} {step===1&&<ChoiceGrid title="Choose a class" intro="Your class describes your training, talents, and the approaches your hero is likely to take." choices={CLASSES} value={draft.class} onChange={value=>setDraft({...draft,class:value})}/>} {step===2&&<DescribeStep draft={draft} setDraft={setDraft}/>} {step===3&&<SkillsStep draft={draft} toggleSkill={toggleSkill} raceSkills={raceSkills} classSkills={classSkills} raceCount={raceCount} classCount={classCount} maxStamina={maxStamina}/>} {step===4&&<Review draft={draft} maxStamina={maxStamina}/>}</div><div className="p-4 sm:px-6 border-t border-[#cbb99b] bg-[#eee4cf]/80 flex justify-between gap-3"><button disabled={step===0} onClick={()=>setStep(step-1)} className="px-4 py-2 rounded-lg font-bold text-[#5c4a3d] disabled:opacity-30 flex items-center gap-1"><ArrowLeft size={17}/> Back</button>{step<4?<button disabled={!canContinue} onClick={()=>setStep(step+1)} className="px-5 py-2.5 rounded-lg bg-[#762b2b] text-white font-bold disabled:opacity-40 flex items-center gap-2">Continue <ArrowRight size={17}/></button>:<button onClick={finish} className="px-5 py-2.5 rounded-lg bg-[#762b2b] text-white font-bold flex items-center gap-2"><Check size={18}/> Add Hero</button>}</div></div></div>}
function ChoiceGrid({title,intro,choices,value,onChange}){return <div><div className="rule-flourish"><h3 className="display-serif text-2xl font-bold text-[#3f3028]">{title}</h3></div><p className="text-sm text-[#75665b] text-center mt-2 mb-5">{intro}</p><div className="grid sm:grid-cols-2 gap-3">{Object.entries(choices).map(([name,info])=><button key={name} onClick={()=>onChange(name)} className={`text-left p-4 rounded-xl border transition-all ${value===name?'border-[#8b4b42] bg-[#f1e1d5] shadow-sm':'border-[#d8ccb6] bg-[#fffdf5]/70 hover:border-[#bda98a]'}`}><div className="flex justify-between gap-2"><span className="display-serif font-bold text-xl text-[#49382f]">{name}</span>{value===name&&<Check size={18} className="text-[#7b2d2d]"/>}</div><p className="text-sm italic text-[#75665b] mt-1">{info.tagline}</p><p className="text-xs text-[#7b2d2d] mt-3"><strong>Suggested:</strong> {info.skills.join(', ')}</p></button>)}</div></div>}
function DescribeStep({draft,setDraft}){const field=(key,value)=>setDraft(c=>({...c,[key]:value}));return <div className="space-y-5"><div className="text-center"><h3 className="display-serif text-2xl font-bold text-[#3f3028]">Who are you?</h3><p className="text-sm text-[#75665b] mt-1">Give your hero enough detail to make their choices feel like their own.</p></div><div className="grid sm:grid-cols-3 gap-3"><Input label="Name" value={draft.name} onChange={v=>field('name',v)} placeholder="Hero name"/><Input label="Age" value={draft.age} onChange={v=>field('age',v)} placeholder="Age"/><Input label="Gender" value={draft.gender} onChange={v=>field('gender',v)} placeholder="Gender"/></div><TextArea label="Physical Description" value={draft.physicalDescription} onChange={v=>field('physicalDescription',v)} placeholder="Appearance, clothing, bearing, scars, mannerisms…"/><TextArea label="Personality & Ideals" value={draft.personality} onChange={v=>field('personality',v)} placeholder="Traits, motivations, beliefs, or ideals…"/><TextArea label="Background" value={draft.background} onChange={v=>field('background',v)} placeholder="Where are you from? What shaped you? Why are you a hero?"/></div>}
function Input({label,value,onChange,placeholder}){return <label className="block"><span className="ink-label text-[8px] text-[#75665b]">{label}</span><input value={value} onChange={e=>onChange(e.target.value)} placeholder={placeholder} className="mt-1 w-full bg-[#fffdf5] border border-[#d2c2a5] rounded-lg px-3 py-2.5 outline-none focus:border-[#8c5a50]"/></label>};function TextArea({label,value,onChange,placeholder}){return <label className="block"><span className="display-serif font-bold text-[#49382f]">{label}</span><textarea value={value} onChange={e=>onChange(e.target.value)} placeholder={placeholder} className="mt-1 w-full min-h-24 bg-[#fffdf5] border border-[#d2c2a5] rounded-xl p-3 outline-none focus:border-[#8c5a50]"/></label>}
function SkillsStep({draft,toggleSkill,raceSkills,classSkills,raceCount,classCount,maxStamina}){return <div><div className="flex items-start justify-between gap-4 mb-4"><div><h3 className="display-serif text-2xl font-bold text-[#3f3028]">Choose 5–8 skills</h3><p className="text-sm text-[#75665b] mt-1">Choose at least two suggested by your race and two suggested by your class.</p></div><div className="shrink-0 text-center bg-[#efe3c8] border border-[#d0bd96] rounded-xl px-3 py-2"><div className="ink-label text-[7px] text-[#75665b]">Stamina</div><div className="display-serif text-2xl font-bold text-[#7b2d2d]">{maxStamina}</div></div></div><div className="grid grid-cols-3 gap-2 mb-5 text-center text-xs"><RuleStatus label="Skills" value={`${draft.skills.length}/5–8`} ok={draft.skills.length>=5&&draft.skills.length<=8}/><RuleStatus label="Race" value={`${raceCount}/2`} ok={raceCount>=2}/><RuleStatus label="Class" value={`${classCount}/2`} ok={classCount>=2}/></div><div className="space-y-4">{Object.entries(SKILLS).map(([category,skills])=><div key={category}><h4 className="ink-label text-[8px] text-[#75665b] mb-2">{category}</h4><div className="grid grid-cols-2 sm:grid-cols-3 gap-2">{skills.map(skill=>{const selected=draft.skills.includes(skill);const race=raceSkills.includes(skill);const cls=classSkills.includes(skill);return <button key={skill} onClick={()=>toggleSkill(skill)} className={`p-2.5 rounded-lg border text-left ${selected?'border-[#8b4b42] bg-[#f1e1d5]':'border-[#d8ccb6] bg-[#fffdf5]/70'}`}><div className="flex items-center justify-between"><span className="font-bold text-sm text-[#49382f]">{skill}</span>{selected&&<Check size={15} className="text-[#7b2d2d]"/>}</div>{(race||cls)&&<div className="text-[10px] text-[#8b4b42] mt-1">{[race&&draft.race,cls&&draft.class].filter(Boolean).join(' · ')}</div>}</button>})}</div></div>)}</div></div>}
function RuleStatus({label,value,ok}){return <div className={`rounded-lg p-2 border ${ok?'bg-[#e7eadc] border-[#b8c09e]':'bg-[#fffdf5]/70 border-[#d2c2a5]'}`}><div className="font-bold text-[#49382f]">{label}</div><div className={ok?'text-[#596647]':'text-[#7b2d2d]'}>{value}</div></div>}
function Review({draft,maxStamina}){return <div><div className="text-center pb-5 border-b border-[#d2c2a5]"><Sparkles size={21} className="mx-auto text-[#9b743b] mb-2"/><div className="ink-label text-[8px] text-[#8b4b42]">Ready for the road</div><h3 className="display-serif text-4xl font-bold text-[#3f3028] mt-1">{draft.name}</h3><p className="ink-label text-[8px] text-[#7b2d2d] mt-2">{draft.race} · {draft.class}</p>{(draft.age||draft.gender)&&<p className="text-sm italic text-[#75665b] mt-2">{[draft.age&&`Age ${draft.age}`,draft.gender].filter(Boolean).join(' · ')}</p>}</div><div className="grid grid-cols-2 gap-3 my-5"><div className="bg-[#fffdf5]/70 border border-[#d2c2a5] rounded-xl p-3 text-center"><Shield className="mx-auto text-[#7b2d2d]" size={18}/><div className="ink-label text-[7px] text-[#75665b] mt-1">Stamina</div><div className="display-serif text-2xl font-bold text-[#49382f]">{maxStamina}</div></div><div className="bg-[#fffdf5]/70 border border-[#d2c2a5] rounded-xl p-3 text-center"><div className="ink-label text-[7px] text-[#75665b]">Skills</div><div className="display-serif text-2xl font-bold text-[#49382f] mt-1">{draft.skills.length}</div></div></div><ReviewSection title="Skills">{draft.skills.join(' · ')}</ReviewSection>{draft.personality&&<ReviewSection title="Personality & Ideals">{draft.personality}</ReviewSection>}{draft.background&&<ReviewSection title="Background">{draft.background}</ReviewSection>}</div>};function ReviewSection({title,children}){return <section className="mt-4"><div className="display-serif font-bold text-[#49382f]">{title}</div><p className="text-sm text-[#75665b] whitespace-pre-wrap mt-1 leading-relaxed">{children}</p></section>}
