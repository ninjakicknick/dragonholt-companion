# Counter control

Numeric increment/decrement behavior is now available as a presentation primitive rather than repeated button markup. It clamps at a configured minimum and can optionally expose direct numeric entry.
