# Story Point grid

The A1–Z8 matrix is extracted as a controlled component. It receives the selected points and a toggle callback, which keeps it independent from storage and makes it suitable for reuse in both the Party screen and future quick-action interfaces.
