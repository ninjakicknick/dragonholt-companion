# Accessibility baseline

New interactive components should expose meaningful labels and state without requiring visual context. The Story Point grid now exposes `aria-pressed`, and extracted numeric controls have explicit accessible labels.
