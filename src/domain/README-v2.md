# Campaign domain

Keep transformations here deterministic and testable. React hooks may call these helpers, but helpers should never call React hooks. Future milestone/reward logic belongs here after it is verified against the game reference material.
