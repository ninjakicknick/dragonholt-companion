# Domain layer

Domain helpers are pure functions with no React, DOM, storage, or network dependencies. This is where reusable campaign rules and value transformations belong as the companion becomes rules-aware.
